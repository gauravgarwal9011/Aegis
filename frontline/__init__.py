"""Frontline: a reliable, provable customer-message triage system.

Orchestrated with LangGraph, traced with Pydantic Logfire, backed by a scored
eval suite. Default LLM provider is an offline deterministic mock so the whole
project runs end-to-end with zero setup / no API keys.
"""

__version__ = "1.0.0"
